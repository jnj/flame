/* This tests for a few valid arithmetic expressions */

fun foo(a:int,b:int)
    x:int;
    y:int;
    z:int;
    w:int;
    xx:int[50];
    i:int;
    f:float;
    g:int;
    h:int;
    c:int;
    d:int;
    e:int;
    begin
        x := 3 + 4;
        y := a * b;
        z := (x + y);
        w := -y;
        w := +y;
        w := xx[3*z];
        w := a - b;
        w := a / b;
        i := int(2*x);
        f := float(3*y);
	g := a*(b+c*(d+e*(int(f) +-+-+-+-+ g)-h))

    end


        
